openai>=1.0.0
python-dotenv>=0.19.0
win10toast>=0.9.0; platform_system=="Windows"
libnotify-bin; platform_system=="Linux"

# Note: On macOS, terminal-notifier is required for notifications.
# Run 'python setup.py' to install it automatically. 